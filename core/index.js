import {backarrow, bgdots, logo, logonotext, marker, gift} from './assets';
import {theme} from './theme';

export default {
  backarrow,
  bgdots,
  logo,
  logonotext,
  theme,
  marker,
  gift,
};
